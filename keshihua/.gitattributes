# 所有文本文件统一用 LF（Git 内部存储），系统自动处理换行符
* text=auto

# 强制前端代码使用 LF（避免 Windows CRLF 污染）
*.js text eol=lf
*.ts text eol=lf
*.html text eol=lf
*.css text eol=lf
*.json text eol=lf
*.py text eol=lf
*.sh text eol=lf

# 忽略二进制文件（不做换行转换）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.zip binary
